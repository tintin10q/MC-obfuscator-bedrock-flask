1. detect all 'scoreboard objective add "" dummy' commands and change the names to random strings
2. multiply scores adding and removing of the same score by the same random amount
3. detect all the function names and rename them with rename functions with random strings in and outside of the function
4. move all functions out of sub folders
5. give each command it's own function with a random name and then it would be command and next function call
6. for each folder add a fake objective and sometimes have 2 function calls 

all changes are recorded in a json and can be restored with the "de-obsucatoin key"



ik houw van apen jo!
